# cargo-audit configuration
# https://docs.rs/cargo-audit/latest/cargo_audit/config/

[advisories]
# No advisory exceptions. RUSTSEC-2026-0235 (rkyv 0.7) left Cargo.lock on 2026-09-24 with
# rust_decimal 1.43.0, which no longer declares an rkyv dependency. `paste` (RUSTSEC-2024-0436)
# is reported as unmaintained, a warning that doesn't fail the audit; it is a compile-time
# macro dependency with no runtime code. Add an exception here only with the reason it is
# inactive and a review date.
ignore = []
