# Dedicated input contract, OwnTone 29.3 src/conffile.c sec_library.
# Tests replace @PIPE_DIRECTORY@ with the absolute FIFO parent.
library {
    directories = { "@PIPE_DIRECTORY@" }
    pipe_autostart = true
    pipe_sample_rate = 44100
    pipe_bits_per_sample = 16
}
